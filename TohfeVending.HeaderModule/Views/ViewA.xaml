<UserControl x:Class="TohfeVending.HeaderModule.Views.ViewA"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:TohfeVending.HeaderModule.Views"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="70" d:DesignWidth="300"
             xmlns:prism="http://prismlibrary.com/"
             prism:ViewModelLocator.AutoWireViewModel="True" >
    <DockPanel  LastChildFill="True">
        <Image Source="{Binding Avatar}" VerticalAlignment="Center" DockPanel.Dock="Right" Width="40" Height="40" />
        <TextBlock Text="{Binding UserInfo.UserName}" VerticalAlignment="Top" DockPanel.Dock="Right" Width="100" />
        <TextBlock Text="{Binding Message}" VerticalAlignment="Center" FontSize="28" />
    </DockPanel>
</UserControl>
